# سجل التغييرات (Project Change Log)

جميع التعديلات والخطوات البرمجية والتوثيقية لمشروع الصيدلية (خادم وعميل).

## [2.25.0] - 2026-09-19
### أُضيف ووُثّق وأُنتج (Added, Documented & Generated)
- **تجهيز وتوليد العرض التقديمي الأكاديمي لمناقشة المشروع (Academic Defense Presentations):**
  - **ملف العرض التقديمي PowerPoint:** توليد ملف العرض التقديمي الرسمي [`docs/PharmaConnect_Defense_Presentation.pptx`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/docs/PharmaConnect_Defense_Presentation.pptx) بنمط عريض (16:9) وهوية بصرية طبية زمردية تضم 10 شرائح هندسية جاهزة للعرض المباشر على جهاز العرض (Data Show).
  - **عرض الويب التفاعلي الفوري:** إنشاء ملف [`docs/presentation.html`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/docs/presentation.html) لعرض الشرائح تفاعلياً من أي متصفح (Full Screen F11، التحكم بالأسهم، شريط التقدم، درج قراءة الملاحظات الصوتية بالضغط على حرف S).
  - **دليل النصوص والشرائح المكتوبة:** إنشاء ملف [`docs/PRESENTATION_SLIDES.md`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/docs/PRESENTATION_SLIDES.md) الذي يضم سيناريو الإلقاء دقيقة بدقيقة لكل متحدث وبطاقة إجابات البرق السريعة.

## [2.24.0] - 2026-09-19
### أُصلح وحُسّن وطُوّر (Fixed, Enhanced & Resolved)
- **القضاء التام على استثناء شجرة فلاتر (`setState() or markNeedsBuild() called during build`):**
  - تم إصلاح استدعاء `notifyListeners()` المتزامن داخل `LocationService.fetchCurrentLocation()` بترحيله بأمان عبر `WidgetsBinding.instance.addPostFrameCallback`.
  - تم تغليف استدعاء تحديد الموقع في `_PharmacyRouteMapWidgetState.initState()` بواسطة `addPostFrameCallback` لضمان عدم إثارة أي استثناء يوقف بناء الشجرة أو يجمّد واجهة العميل عند فتح تذكرة الحجز.
- **المزامنة اللحظية الفورية لتحديث حالة الحجز وتسليم الدواء في تطبيق الموبايل والعميل:**
  - تم تصحيح منطق استعلام تفاصيل الحجز `getReservationDetails` في [`ApiService.dart`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/core/network/api_service.dart) لمعالجة حالة 404 بدقة، ومنع إعادة الكاش المحلي المجمّد بحالة معلقة (`pending`).
  - تم دعم الاستعلام بالهاتف ورموز الحجز معاً في واجهة `/api/v1/reservations/my` دون خروج مبكر فارغ، لضمان استرجاع حجوزات الزوار فور إنشائها.
  - تم تطوير دالة فحص وتحديث الحالة `_checkServerStatus()` في [`ReservationPassScreen`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/presentation/screens/reservation_pass_screen.dart) لمطابقة الرموز المؤقتة فورياً بالحجز الحقيقي المكتمل على السيرفر المركزي بمجرد تأكيد الصيدلي في لوحة التحكم، وإلغاء مؤقت العد التنازلي وإظهار شارة الاستلام الخضراء المكتملة فوراً.
  - زيادة مهلة اتصال إنشاء الحجز إلى 35 ثانية لمنع اللجوء للرموز المحلية المؤقتة في فترات استيقاظ السيرفر السحابي.

## [2.23.0] - 2026-09-19
### أُصلح وحُسّن (Fixed & Enhanced)
- **القضاء التام على اختفاء الحجوزات بعد ظهورها المؤقت (Fix Temporary Reservation Disappearance):**
  - **التعرف التلقائي على توكن Sanctum بالمسارات المشتركة (`resolveUser`):** تم حل المشكلة الجذرية المتمثلة في أن مسارات الحجز كانت خارج middleware `auth:sanctum`، مما جعل دالة `$request->user()` تعيد `null` دائماً حتى مع إرسال توكن المصادقة في ترويسة الطلب. تم بناء دالة `resolveUser` للتحقق المباشر من `PersonalAccessToken` واستخراج المريض الفعلي بدقة، مما يضمن تسجيل الحجز فورياً في قاعدة البيانات تحت حسابه الصحيح واسترجاعه دائماً بنجاح.
  - **التبني التلقائي لحجوزات الزوار السابقة (Guest Reservation Adoption):** عند استعلام المريض المسجل، يقوم الخادم تلقائياً بربط أي حجوزات ضيف سابقة كانت مسجلة برموز على هذا الجهاز بحساب المريض المعتمد (`update(['user_id' => $user->id])`)، مما يمنع ضياع أي طلب حجز أنشئ قبل إتمام تسجيل الدخول.
  - **حماية الحجوزات المحلية النشطة في الذاكرة التخزينية (Safe Reservation Cache Retention):** تم تحديث `getMyReservations()` في [`ApiService.dart`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/core/network/api_service.dart) بحيث لا يتم حذف أي حجز تم إنشاؤه حديثاً (خلال آخر 3 دقائق) بشكل متسرع إذا تأخرت استجابة السيرفر أو واجهت الشبكة بطء تزامن، مما يمنع ظاهرة "ظهور الحجز لثوانٍ ثم اختفائه".

## [2.22.0] - 2026-09-19
### أُصلح وحُسّن وأُمّن (Fixed, Enhanced & Secured)
- **العزل التام والمطلق لبيانات وحجوزات المستخدمين والضيوف (Strict User Session & Reservation Isolation):**
  - **معالجة تداخل الحسابات عند إنشاء حساب جديد:** تم ضبط نقطة نهاية استعراض الحجوزات `myReservations` بملف [`ReservationApiController.php`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/backend/app/Http/Controllers/Api/V1/ReservationApiController.php) لإعطاء الأولوية المطلقة للمستخدم المصادق بالتوكن (`$request->user()`)، بحيث لا يعود يرى سوى حجوزاته الشخصية المسجلة برقم معرفه الفريد (`user_id`).
  - **حذف الاستعلام الافتراضي العام للضيوف:** تم إلغاء الكود القديم `Reservation::latest()->take(20)->get()` نهائياً، والذي كان يعيد أحدث 20 حجزاً من قاعدة البيانات لمرضى آخرين عند فتح الشاشة كزائر أو قبل استكمال التوثيق. أصبح النظام يعيد قائمة فارغة تماماً ومؤمنة في حال عدم وجود حجوزات للمستخدم.
  - **منع تشارك وتعديل سجل المستخدم في حجوزات الزوار:** تم تعديل منطق إنشاء الحجز بالخادم لمنع استخدام هاتف افتراضي مشترك (`+967 770 000 000`) كان يتشاركه جميع الزوار وتتغير أسماؤهم عليه، وأصبح لكل حجز زائر هويته المستقلة المعزولة دون مساس بأي حساب مستخدم مسجل مسبقاً.
  - **تفريغ الكاش المحلي التلقائي عند التسجيل والدخول والخروج:** تم تحديث [`ApiService`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/core/network/api_service.dart) بتطبيق `_cachedReservations.clear()` فور تسجيل حساب جديد (`register`)، أو تسجيل الدخول (`login`)، أو تسجيل الخروج (`logout`)، مع إيقاف تمرير رموز الكاش القديمة في استعلام المستخدم المسجل، مما يضمن بدء أي مستخدم جديد بصفحة نظيفة 100% تخلو من حجوزات الآخرين.
  - **إضافة اختبارات آلية للتحقق من العزل الأمني:** إضافة الاختبار البرمجي `test_my_reservations_strictly_isolates_user_data` بملف [`ReservationConcurrencyTest.php`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/backend/tests/Feature/ReservationConcurrencyTest.php) واجتيازه بنجاح بنسبة 100%.

## [2.21.0] - 2026-09-19
### أُصلح وحُسّن وطُوّر (Fixed, Enhanced & Implemented)
- **المزامنة اللحظية الشاملة لحالة تسليم الدواء (Real-time Medicine Pickup Sync):**
  - تصحيح خطأ استعلام العلاقات في نقطة نهاية الحجز بالرمز `/api/v1/reservations/{codeOrId}` بملف [`ReservationApiController.php`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/backend/app/Http/Controllers/Api/V1/ReservationApiController.php) من `items.medicine` إلى `reservationItems.pharmacyMedicine.medicine` وإضافة علاقة `medicine()` على موديل [`ReservationItem.php`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/backend/app/Models/ReservationItem.php).
  - إضافة دعم استعلام الحجوزات بالرموز المحفوظة محلياً عبر المعامل `?codes=...` في نقطة `/api/v1/reservations/my` لضمان إرجاع كافة حجوزات الضيوف والهواتف المختلفة بدقة 100%.
  - تفعيل مؤقت المزامنة الدورية اللحظية (Real-time Polling كل 2.5 ثانية) في شاشة التذكرة [`ReservationPassScreen`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/presentation/screens/reservation_pass_screen.dart): فور تأكيد الصيدلي في المتصفح يتم إلغاء مؤقت الـ TTL فوراً وإظهار شارة وبطاقة النجاح الزمردية: "تم استلام الدواء بنجاح والمحاسبة ✓" مع تنبيه اهتزازي ولمسي فوري.
  - إضافة استماع تلقائي ومزامنة خلفية صامتة كل 3.5 ثوانٍ في شاشة [`MyReservationsScreen`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/presentation/screens/my_reservations_screen.dart) لضمان تحديث الحالات فور تغيرها دون الحاجة للريفرش اليدوي.

- **حل مشكلة ظهور الحجز باسم "صيدلية الشفاء" عند الحجز من صيدليات مختلفة:**
  - رفع مهلة انتظار استجابة الخادم المركزي في [`ApiService.createReservation`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/core/network/api_service.dart) من 4 ثوانٍ إلى 15 ثانية كاملة لاستيعاب بطء الشبكات أو فترات استيقاظ السيرفر السحابي (Cold Start).
  - تمرير بيانات الصيدلية والدواء والسعر الفعلي المختار من [`MedicineDetailsScreen`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/frontend/lib/presentation/screens/medicine_details_screen.dart) إلى `createReservation` لضمان اعتماد الصيدلية الحقيقية حتى في أقصى حالات انقطاع الشبكة.

- **القضاء التام على خطأ `419 | PAGE EXPIRED` عند تسجيل الخروج أو انتهاء وقت الجلسة:**
  - استثناء مسار `logout` من فحص الـ CSRF في [`bootstrap/app.php`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/backend/bootstrap/app.php).
  - دعم كلا طريقتي `GET` و `POST` لمسار `/logout` في [`routes/web.php`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/backend/routes/web.php).
  - التقاط استثناء `TokenMismatchException` وإعادة توجيه المستخدم لصفحة تسجيل الدخول مع رسالة تنبيه واضحة بدلاً من إظهار شاشة الخطأ السوداء.

- **تفعيل المزامنة الحية التلقائية في لوحة تحكم الصيدلية (Auto-Refresh Live Polling):**
  - إضافة شريط المزامنة الحية مع نقطة خضراء نابضة في صفحة [`reservations.blade.php`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/backend/resources/views/pharmacy/reservations.blade.php).
  - إضافة كود AJAX ذكي يقوم بجلب وتحديث جدول الحجوزات الواردة تلقائياً كل 5 ثوانٍ، مما يتيح للصيدلي رؤية طلبات الحجز الجديدة فور صدورها من تطبيق الموبايل دون الحاجة لإعادة تحميل الصفحة يدوياً (F5).

## [2.20.0] - 2026-09-19
### أُصلح وحُسّن (Fixed & Enhanced)
- **معالجة تحذير أمان النماذج في المتصفح ("The information you're about to submit is not secure"):**
  - تفعيل الثقة التامة في البروكسي العكسي لمنصة Render السحابية عبر `$middleware->trustProxies(at: '*')` في `bootstrap/app.php`.
  - تطبيق `URL::forceScheme('https')` في `AppServiceProvider.php` لضمان توليد كافة روابط النماذج وإعادة التوجيه (Redirects / Form Actions) ببروتوكول `https://` المشفر حصرياً عند العمل خلف البروكسي السحابي.

## [2.19.0] - 2026-09-19
### أضيف وتوثق (Added & Documented)
- **بناء وتصدير حزمة الأندرويد النهائية بنجاح (`PharmaConnect.apk`):**
  - تصدير حزمة الإنتاج `app-release.apk` بحجم **52.1 ميجابايت** ونسخها مباشرة في جذر المشروع باسم [`PharmaConnect.apk`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/PharmaConnect.apk).
  - ضبط أذونات الإنترنت والموقع الجغرافي (`INTERNET`, `ACCESS_NETWORK_STATE`, `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION`, `usesCleartextTraffic=true`) في `AndroidManifest.xml`.
  - تفعيل الاتصال المباشر التلقائي مع السيرفر السحابي الحي على منصة Render:
    `https://pharmaconnect-dso7.onrender.com/api/v1` عبر `useCloudBackend = true`.
  - معالجة مشكلة المسارات غير الإنجليزية على ويندوز عبر حيلة القرص الافتراضي النقي (`subst X:`) وضبط توافق منصة `Android SDK 35`.
  - إضافة مستودع المرايا السريع `maven.aliyun.com/repository/google` لتجاوز بطء وانقطاع شبكات التحميل العالمية.

## [2.18.0] - 2026-09-19
### أضيف وهُيّئ (Added & Configured)
- **تهيئة النشر السحابي المجاني الكامل على منصة Render.com (`render.yaml` & `Dockerfile`):**
  - إنشاء ملف البناء السحابي `render.yaml` في جذر المشروع لدعم النشر الآلي بضغطة واحدة (Blueprint Deploy).
  - إنشاء `backend/Dockerfile` و `backend/docker-entrypoint.sh` لبيئة إنتاج خفيفة تعتمد على Alpine Linux و PHP 8.2 مع تشغيل آلي للـ Migrations والـ Seeders.
  - إضافة `backend/.dockerignore` لتقليص زمن الرفع والبناء السحابي.
  - تحديث `frontend/lib/core/constants/app_constants.dart` بدعم التبديل السلس بين الخادم المحلي والسيرفر السحابي المباشر عبر `useCloudBackend`.
  - كتابة دليل النشر السحابي المجاني خطوة بخطوة في [`docs/RENDER_DEPLOYMENT_GUIDE.md`](file:///d:/IT%20FILES/level%204/خادم%20وعميل/مشروع%20الصيدلية/docs/RENDER_DEPLOYMENT_GUIDE.md).

## [2.17.0] - 2026-09-19
### أضيف وتوثق (Added & Documented)
- **إنشاء ورقة المراجعة السريعة والمكثفة للمناقشة (`docs/CHEAT_SHEET.md`):**
  - بطاقة الإنقاذ السريع (Defense Cheat Sheet) أمام لجان التحكيم الأكاديمي لمقرر **برمجة خادم وعميل (Level 4)**.
  - ملخص المصعد (Elevator Pitch) في 30 ثانية لشرح المشروع بشكل احترافي.
  - مصفوفة الأرقام التقنية الحاسمة (الجداول السبعة، المسارات الـ 12، 47 اختباراً، معدل الخطأ 0%).
  - جدول محدد الأكواد (File Locator) لإظهار كود هافرسين، وقفل التزامن، والـ TTL، والـ B2B API في ثوانٍ.
  - مصفوفة الـ REST API السريعة بكافة الـ Endpoints، والترويسات، ومعاملات الـ JSON، ورموز الاستجابة.
  - ملخص هيكل قاعدة البيانات بالشكل المعياري الثالث (3NF) والعلاقات الأساسية.
  - إجابات البرق (Lightning Answers) لأهم 10 أسئلة فخاخ شائعة في مناقشة مادة خادم وعميل.
  - أوامر التيرمينال السريعة للتشغيل والاختبار والفحص بـ Pint و Analyze.
  - قائمة فحص العرض الحي في 3 دقائق (3-Minute Live Demo Checklist) خطوة بخطوة.

## [2.16.0] - 2026-09-19
### أضيف وتوثق (Added & Documented)
- **إنشاء دليل المناقشة الشامل والمرجع البرمجي الكامل لمشروع PharmaConnect (`docs/PROJECT_GUIDE.md`):**
  - دليل أكاديمي وتقني تفصيلي بمقرر **برمجة خادم وعميل (Client-Server Programming - Level 4)** يزيد عن 1000 سطر.
  - تفصيل الفلسفة المعمارية ونموذج خادم وعميل الموزع متعدد الطبقات (3-Tier Distributed System)، معمارية الـ RESTful API، الترويسات، وحالات استجابة HTTP.
  - تفصيل مخطط قاعدة البيانات بالشكل المعياري الثالث (3NF) والجداول السبعة الأساسية مع أكواد الـ Migrations ونماذج الـ Eloquent.
  - تفصيل وشرح أكواد الخادم المركزي (Laravel) بالكامل: المسارات `routes/api.php`، المصادقة والتوكنات (Sanctum)، خوارزمية هافرسين لحساب المسافات الجغرافية، خدمة الحجز ومعالجة الـ Race Conditions بالقفل التشاؤمي والمعاملات الذرية (`lockForUpdate` و `DB::transaction`)، وبوابة تكامل أنظمة الصيدليات المحاسبية (B2B Partner Integration API).
  - تفصيل وشرح أكواد عميل الهاتف (Flutter) بالكامل: معمارية Clean Architecture، نمط Singleton + ChangeNotifier في `ApiService`، نماذج تحويل الـ JSON، بطاقات الأدوية وصورها، تذكرة الحجز ومؤقت الـ TTL اللحظي، خريطة الطرق الحقيقية بدون مفاتيح تجارية عبر مربعات CartoDB / OpenStreetMap، وشاشة بوابة الصيدليات.
  - مخطط تسلسلي متكامل (Sequence Diagram) يوضح رحلة الطلب بين العميل والخادم وقاعدة البيانات من البحث إلى الحجز والتسليم.
  - بنك يضم 10 أسئلة تقنية نموذجية متوقعة في لجان مناقشة مادة خادم وعميل مع إجاباتها الأكاديمية المفصلة.
  - مراجع علمية موثقة وفق الإصدار السابع للجمعية النفسية الأمريكية (APA 7th Edition).

## [2.15.0] - 2026-09-19
### أُصلح وحُسّن (Fixed & Enhanced)
- **إصلاح overflow الـ AppBar (الشريط الأصفر/الأسود):**
  - إزالة `Row` المتداخل غير الآمن داخل `title` واستبداله بـ `Flexible` + `titleSpacing: 0` لمنع الـ overflow في وضع RTL.
  - تصغير حجم الخط من 20 إلى 18 وأيقونة الصيدلية من 20 إلى 18 لتوفير مساحة كافية.
  - إضافة زر قائمة جانبية صريح (`menu_rounded`) لفتح Drawer في AppBar.
- **إصلاح overflow في خريطة المسار (PharmacyRouteMapWidget):**
  - استبدال `Row{spaceBetween} -> Row{children}` بـ `Row -> Expanded -> Column` لضمان أن نص الخريطة لا يتجاوز حدود العرض المتاح.
  - إضافة `overflow: TextOverflow.ellipsis` للنصوص الطويلة داخل الكارد.
- **إصلاح عداد الحجوزات (Badge) - لا يُحدَّث إلا عند الدخول للشاشة:**
  - تحويل `ApiService` من `class` عادية إلى `ChangeNotifier` تُطلق `notifyListeners()` عند كل تغيير في `_cachedReservations` (عند الإنشاء وعند الجلب).
  - إضافة `Timer.periodic(1 ثانية)` عالمي في `_HomeScreenState.initState` يستدعي `setState` بشكل دوري لتحديث العداد التنازلي حتى خارج شاشة الحجوزات.
  - إضافة `ApiService().addListener(_onApiChange)` لتحديث الـ Badge فور إضافة حجز جديد.
  - تنظيف التبعيات بشكل صحيح في `dispose()`.

## [2.14.0] - 2026-09-19
### أضيف وحُسّن وأُصلح (Added, Enhanced & Fixed)
- **إنشاء شاشة طلب API للصيدليات (PharmacyApiScreen) - ميزة جديدة:**
  - شاشة متكاملة بـ 3 تبويبات: (1) طلب ربط مع نموذج بيانات صيدلية وخيارات خطط اشتراك تفاعلية. (2) توثيق API مع Quick Start، مزامنة المخزون، Webhooks، وقواعد الأمان. (3) قائمة Endpoints كاملة مع HTTP Methods ملوّنة.
  - ربط الشاشة بالقائمة الجانبية (Drawer) تحت قسم "الدعم والمعلومات" بزر جديد بأيقونة `api_rounded`.
  - إضافة تأثير نجاح إرسال الطلب مع عرض مفتاح API توضيحي (Demo Key) مع زر نسخ.
  - جميع الـ Endpoints قابلة للنسخ بنقرة واحدة (`Clipboard`) مع SnackBar تأكيد.
- **تحسين بطاقات الأدوية في نتائج البحث (Medicine Cards Redesign):**
  - إضافة صورة الدواء في أعلى كل بطاقة: إظهار `Image.network` من `imageUrl` عند توفره.
  - إضافة `Placeholder` جمالي متدرج (Gradient Placeholder) مع أيقونة `medication_rounded` عند غياب الصورة.
  - تحسين هيكل البطاقة: بطاقة داخلية لبيانات الصيدلية، فصل واضح بين اسم الدواء والسعر وزر الحجز.
  - إصلاح ألوان النصوص في الوضع الليلي (Dark Mode) لعنوان نتائج البحث وعدد النتائج.
- **تحسين القائمة الجانبية (Drawer):**
  - إضافة عنصر جديد "ربط الصيدلية بـ PharmaConnect" مع أيقونة API.
  - تحديث نص عنصر "الدعم" ليكون أكثر وضوحاً.

## [2.13.0] - 2026-09-19
### أضيف وحُسّن وأُصلح (Added, Enhanced & Fixed)
- **إعادة تصميم واجهة تذكرة الحجز لتصبح تذكرة طبية فاخرة (Luxury Medical Boarding Pass UI):**
  - استبدال التصميم الأحادي القاتم ببطاقة تذكرة طبية رقمية راقية مستوحاة من تذاكر السفر والاعتماد الصحي (Medical Boarding Pass).
  - إضافة شارة حالة تفاعلية في الترويسة بنقطة زمردية وميضية توضح حالة التذكرة بدقة («تذكرة حجز دوائي صالحة ونشطة»).
  - إضافة إطار ماسح ضوئي ذكي لرمز الـ QR بأربع زوايا استهداف بصرية وتوهج شعاعي زمردي مريح للعين.
  - إبراز كود الحجز بخط أحادي المسافة (`monospace`) مع زر سريع لنسخ الرمز (`Clipboard`) بلمسة واحدة وظهور تنبيه نجاح النسخ.
  - إضافة كبسولة توقيت تنازلي متوهجة بالأخضر الزمردي الرقمي مع أيقونة منبه توضح الدقائق والثواني المتبقية حتى انتهاء المهلة.
  - إضافة خط التمزيق والتثقيب التذكاري الفاخر (Perforated Tear Line) بفتحتين نصف دائريتين على الحافتين مع خط منقط متصل يفصل بين بيانات الاعتماد وتفاصيل الاستلام.
  - تصميم جدول أصناف الأدوية المحجوزة بأيقونات طبية مصغرة وشارات للكميات، مع شريط إجمالي نهائي مميز.
  - إضافة زر اتصال مباشر بالصيدلية عبر الهاتف (`tel:`) للتواصل السريع بنقرة واحدة.

- **عرض الخريطة الحقيقية وبلاطات الشوارع الفعلية (Real OpenStreetMap & CartoDB Tile Engine):**
  - التخلص التام من الشبكة الوهمية المرسومة يدوياً، واستبدالها بمحرك خرائط رقمي حقيقي يعرض بلاطات الشوارع الحقيقية (Real Slippy Map Tiles) بدقة تقريب Zoom 15.
  - ربط الخريطة بطبقات عالمية مفتوحة موثوقة: CartoDB Voyager للوضع النهاري، و CartoDB Dark Matter الفاخر للوضع الليلي، مما يمنع تماماً ظهور بطاقة بيضاء منفرة داخل الثيم الليلي ويوحد الهوية البصرية.
  - إظهار معالم وشوارع صنعاء الحقيقية (شارع حدة، برج نماء، شارع الستين، إلخ).
  - رسم مسار ملاحي متوهج يصل بين نقطة المريض وموقع الصيدلية.
  - وضع دبوس موقع المريض الفعلي (بأيقونة رادار زرقاء مميزة) ودبوس الصيدلية الزمردي مع اسمها المعتمد.

- **تحديد موقع العميل الفعلي وحساب المسافة بدقة (Live GPS & Actual Haversine Distance):**
  - ربط الخريطة وشاشة التذكرة بخدمة `LocationService` التي تجلب إحداثيات جهاز العميل الحقيقية تلقائياً.
  - حساب المسافة الجغرافية الفاصلة بين موقع العميل الحقيقي والصيدلية بالمعادلة الرياضية الدقيقة (Haversine Formula) بالكيلومتر مع التقريب لأقرب 100 متر.
  - توفير زر تفاعلي لتحديث الموقع الفعلي فورياً (`GPS Refresh`).
  - تحديث تقديرات وقت الوصول بالسيارة ومشياً على الأقدام بناءً على المسافة الفعلية المقاسة.
  - توفير زر مباشر لفتح المسار والملاحة الحية على خرائط جوجل (`Google Maps`) بإحداثيات الانطلاق الحقيقية للعميل والوجهة الدقيقة للصيدلية.

## [2.12.0] - 2026-09-19
### أضيف وحُسّن وأُصلح (Added, Enhanced & Fixed)
- **العد التنازلي الحقيقي المطلق لصلاحية الحجز (Real-Time Absolute Clock Countdown):**
  - **معالجة توقف العداد عند الخروج:**
    - تصحيح مشكلة الاعتماد على عداد ثوانٍ محلي كان يتوقف بمجرد إغلاق الشاشة أو التطبيق.
    - ربط العداد التنازلي مباشرة بالوقت المطلق لساعة النظام (`expiresAt.difference(DateTime.now())`).
    - عند مغادرة الشاشة أو إغلاق التطبيق لأي مدة (مثلاً 5 أو 10 دقائق)، يستمر الوقت بالانقضاء طبيعياً في الواقع، وعند العودة للشاشة يظهر الوقت المتبقي الحقيقي المطابق للواقع بدقة متناهية حتى بالثانية.
  - **تحديث بطاقات قائمة الحجوزات (Live Countdown in My Reservations):**
    - إضافة مؤقت وميضي (`Ticker`) في شاشة `MyReservationsScreen` يقوم بتحديث الثواني والدقائق مباشرة داخل شارات الحجوزات النشطة: مثل `نشط (27:14)`.
    - التحول التلقائي للحالة إلى «منتهي الصلاحية» باللون الأحمر فور تجاوز الوقت الفعلي، دون الحاجة لإعادة الدخول للشاشة.

## [2.11.0] - 2026-09-19
### أضيف وحُسّن وأُصلح (Added, Enhanced & Fixed)
- **إزالة شريط التحذير الأصفر والأسود (Elimination of AppBar Overflow):**
  - معالجة خطأ تجاوز العرض (`RenderFlex overflowed by 7 pixels`) في الشريط العلوي `AppBar`.
  - تحويل عنوان التطبيق إلى عنصر مرن (`Flexible`) مع تقليص المسافات وإزالة أزرار الحجوزات والحساب المكررة من الـ `actions` بعد توفيرها داخل شريط التنقل السفلي والقائمة الجانبية، مما يوفر مساحة كافية ويضمن عدم ظهور أي خطوط تحذيرية صفراء وسوداء إطلاقاً على أي مقاس شاشة.
- **تنسيق وترتيب رقائق الأدوية الشائعة (Symmetrical Grid for Popular Medicines):**
  - استبدال التوزيع العشوائي غير المتناسق (`Wrap`) بشبكة متناظرة ومنظمة مكوّنة من عمودين متساويين (`GridView.builder`).
  - توحيد قياسات وأبعاد وتوسيط أيقونات ونصوص الأدوية الأكثر طلباً (`Panadol Extra`, `Augmentin`, `Norvasc`, `Brufen`) لتبدو كبطاقات أنيقة ومتوازنة ومنظمة 100%.
- **إصلاح وضوح وتباين صندوق البحث في المظهر الليلي (Dark Mode Search Input Fix):**
  - ضبط ألوان حقل البحث في الثيم الليلي ليكون النص المكتوب أبيض صافي وعريض (`Colors.white, FontWeight.w600`) فوق خلفية رمادية كحلية (`#1E293B`) وحدود أردوازية (`#334155`).
  - منع الكتابة البيضاء فوق الخلفية البيضاء، مما يضمن ظهور كل حرف يكتبه المستخدم بوضوح تام وتباين بصري مريح للعين في كلا الوضعين.
- **تحديث وانعكاس الكمية المحجوزة والمتاحة فورياً (Real-time Stock Deduction & Active Badge):**
  - ربط عملية الحجز بتحديث فوري مباشر للكمية المتاحة للصنف في واجهة المستخدم، حيث يتم خصم الكمية المحجوزة لحظياً وتحديث شارة «متوفر X علبة».
  - تفعيل تحديث نتائج البحث تلقائياً عند العودة من شاشة الحجز لتعكس الكميات المتبقية فورياً.
  - إظهار شارة رقمية (Badge) على أيقونة «طلباتي وحجوزاتي» في شريط التنقل السفلي تُظهر عدد الحجوزات النشطة الحالية فور إتمام أي حجز.
  - نشر ملف تكوين الـ CORS الرسمي في خادم Laravel (`config/cors.php`) لتمكين المتصفحات (Chrome) من التواصل المباشر مع واجهات الخادم دون أي حظر للمنافذ.

## [2.10.0] - 2026-09-18
### أضيف وحُسّن (Added & Enhanced)
- **القائمة الجانبية الطبية الشاملة (Side Navigation Drawer):**
  - بناء وتفعيل القائمة الجانبية `_buildAppDrawer` في الشاشة الرئيسية `main.dart` تفتح بسلاسة عبر زر القائمة (`Icons.menu`) والتمرير من اليمين بنظام RTL.
  - تتضمن القائمة:
    - ترويسة متدرجة بالأخضر الزمردي والكحلي الطبي، مع صورة العميل الرمزية والاسم والبريد وشارة التوثيق.
    - روابط سريعة للبحث عن الأدوية، طلباتي وحجوزاتي، والملف الشخصي.
    - مفتاح تبديل المظهر الليلي والنهاري (Switch) مدمج داخل القائمة.
    - نوافذ إرشادية لشرح مهلة الحجز (TTL) ومركز الدعم الفني وربط أنظمة الصيدليات (B2B API).
    - زر تسجيل الدخول أو الخروج مع التوجيه السلس.
- **تصحيح منطق حالة الحجز في تذكرة الاستلام (Delivered & Completed Pass Fix):**
  - تصحيح الخطأ الذي كان يتسبب في ظهور «ملغي آلياً» و«انتهت مهلة الحجز» للحجوزات التي تم تسليمها بالفعل.
  - إيقاف الاعتماد الأحادي على مؤقت الثواني، وفحص حالة الحجز المعتمدة من الخادم (`status == 'completed'`).
  - عند تسليم الحجز، تظهر شارة التذكرة الآن باللون الأخضر الموثق: **«تم الاستلام والتسليم بنجاح ✓»** مع أيقونة التحقق ونص إرشادي يفيد اكتمال العملية بنجاح.
  - إضافة مسار الـ API المباشر `GET /api/v1/reservations/{code}` في الخادم ودالة `getReservationDetails` في Flutter لجلب الحالة الحية وتحديثها فورياً.
  - إضافة زر تحديث حي (Refresh) في شريط الـ AppBar بتذكرة الحجز لمزامنة الحالة بنقرة واحدة.

## [2.9.0] - 2026-09-18
### أضيف وعُدّل (Added & Modified)
- **إمكانية تعديل الملف الشخصي والإعدادات (Interactive Profile & Settings Management):**
  - **واجهة تعديل البيانات الشخصية:**
    - إضافة زر تفاعلي «تعديل البيانات الشخصية» في بطاقة المستخدم بشاشة الملف الشخصي `ProfileScreen`.
    - بناء نافذة سفلية منبثقة (`_showEditProfileModal`) تتيح تعديل الاسم الكامل ورقم الهاتف وتحديثها فورياً مع مؤشر حفظ وتنبيه نجاح.
  - **الخادم المركزي والواجهات البرمجية (Backend RESTful Update API):**
    - إضافة دالة `updateProfile` في `AuthController.php` للتحقق وتحديث بيانات الاسم والهاتف وحفظها في قاعدة البيانات.
    - تسجيل مسار التحديث `PUT /api/v1/auth/profile` الموثق بـ Sanctum، ودعم المسار المباشر `PUT /api/v1/auth/profile/update`.
    - دعم تحديث الكاش والحالة اللحظية محلياً وسحابياً عبر `ApiService().updateProfile()`.
  - **تعديل الإعدادات والتفضيلات التفاعلية:**
    - **محدد النطاق الجغرافي والمدينة:** نافذة منبثقة `_showCitySelector()` تتيح اختيار المدينة (صنعاء، عدن، تعز، إب، حضرموت، الحديدة، ذمار) مع تحديث النطاق الجغرافي للبحث.
    - **مفتاح إشعارات توفر الدواء:** تفعيل وتعطيل فوري للإشعارات بنقرة واحدة مع رسالة تنبيهية.
    - **دليل صلاحية الحجز (TTL):** نافذة معلومات تشرح مهلة الـ 30 دقيقة وقفل التزامن التشاؤمي.

- **تصحيح الصياغات الطبية وتعميم التسمية (Customer & User-Centric Labeling):**
  - استبدال صفة الحصر «مريض مسجل ومعتمد» بـ **«عميل موثّق ومعتمد»** في شارة الملف الشخصي.
  - تعميم الحقول في شاشة الدخول والتسجيل `AuthScreen` لتصبح **«الاسم الكامل للعميل / المستخدم»**.
  - تحديث قسم الحجز في `MedicineDetailsScreen` ليصبح **«بيانات المستلم (لتسجيل الحجز باسمك)»** و **«اسم المستلم / العميل»** ليعكس شمولية التطبيق لكافة الفئات (مشتريات مستلزمات، إسعافات أولية، مكملات، أدوية لأفراد العائلة أو الأصدقاء) بدلاً من قصرها على المرضى فقط.


- **الهندسة الشاملة لتجربة وواجهة المستخدم (Comprehensive UI/UX Medical Overhaul):**
  - **التوطين واتجاه النصوص العربي (Native Arabic RTL):**
    - تفعيل حزمة `flutter_localizations` بتهيئة كاملة للغة العربية والاتجاه من اليمين إلى اليسار (`Locale('ar', 'YE')`).
    - تطبيق محاذاة ومرآة متناسقة لكافة العناصر والأيقونات وأسهم التنقل وفق معايير WCAG 2.1 و Material Design 3.
  - **الطباعة الرقمية والخطوط الطبية (Google Fonts Tajawal):**
    - دمج خط **Tajawal** المعياري العربي الأنيق بجميع أوزانه (عادي، متوسط، عريض، وفائق العرض) عبر `GoogleFonts.tajawalTextTheme()`.
    - تنسيق التباين الطباعي للأحجام والعناوين والبطاقات الطبية لتسهيل قراءة الأسماء التجارية والعلمية للأدوية.
  - **نظام الثيم النهاري والليلي المزدوج (Dual Theme System: Light & Medical Dark):**
    - **الثيم النهاري الطبي (Light Mode):** باللون الزمردي المعتمد (`#059669`)، والأسطح البيضاء الصافية، ولمسات النعناع الهادئة (`#D1FAE5`).
    - **الثيم الليلي الفاخر (Medical Dark Mode):** بخلفية كحلية عميقة (`#0B1120`)، وأسطح بطاقات من حجر الأردواز الطبي (`#1E293B`)، وإضاءات زمردية نيونية (`#34D399`) مريحة للعينين في البيئات المظلمة.
    - **متحكم المظهر العام (ThemeController):** إدارة تفاعلية فورية بنمط Singleton و `ChangeNotifier` تمكن المريض من التبديل الفوري بنقرة واحدة عبر:
      1. زر المظهر السريع (☀️ / 🌙) في الشريط العلوي (AppBar) لشاشتي الرئيسية والدخول.
      2. مفتاح تبديل مخصص داخل شاشة الملف الشخصي `ProfileScreen`.
  - **إعادة تصميم وتنسيق الشاشات وتجنب تجاوزات الحواف (Zero Layout Overflows):**
    - إعادة بناء كافة البطاقات في `HomeScreen`, `ProfileScreen`, `AuthScreen`, `MedicineDetailsScreen`, `ReservationPassScreen`, `MyReservationsScreen` لتستخدم أسطح متكيفة ديناميكياً مع الثيم الفعال (`cardBg`, `borderColor`, `isDark`).
    - معالجة خطأ تجاوز العرض (`RenderFlex overflowed by 35 pixels`) في `MedicineDetailsScreen` بجعل أسطر التفاصيل مرنة وتفاعلية مع المساحة باستخدام `Expanded` و `Flexible`.
  - **معالجة بناء بيئة Windows (Windows Desktop Build Stability):**
    - تجاوز خطأ حزمة `objective_c` المعتمِدة على الميزات التجريبية (Native Assets) عبر تثبيت `path_provider_foundation: 2.4.0` في `dependency_overrides` بما يضمن بناء التطبيق على نظام Windows دون أي تعارضات مسار (`D:\IT FILES`).
  - فحص الكود واجتياز 100% من اختبارات الودجات واختبارات الوحدات والنماذج بنجاح تام.

## [2.7.0] - 2026-09-18
### أضيف (Added)
- **لوحة الإدارة المركزية للنظام ككل (Super Admin Central Portal):**
  - بناء متحكم الإدارة `AdminDashboardController` المخصص للمشرف العام (`role === 'admin'`).
  - تطوير قوالب واجهات الإدارة:
    - `admin/dashboard.blade.php`: إحصائيات النظام اللحظية (الصيدليات، المرضى، الأدوية، الحجوزات)، ومراقبة حية لأحدث العمليات.
    - `admin/pharmacies.blade.php`: إدارة كاملة للصيدليات (تفعيل، تعطيل، توثيق واعتماد، عرض بيانات الترخيص والتواصل).
    - `admin/patients.blade.php`: إدارة مستخدمي تطبيق الهاتف ومتابعة سجل نشاط حجوزاتهم.
    - `admin/medicines.blade.php`: إدارة الفهرس الوطني العام للأدوية مع نموذج إضافة دواء جديد وتصنيفه والباركود.
  - توجيه المشرف العام تلقائياً عند تسجيل الدخول (`admin@pharmaconnect.ye`) إلى `/admin/dashboard`.
- **واجهات ربط أنظمة الصيدليات المحاسبية (Partner B2B Integration API):**
  - بناء `PartnerIntegrationApiController` لتوفير واجهات RESTful موحدة تتيح لبرامج الصيدليات ونقاط البيع (Onyx Pro، المحاسب، إلخ) المزامنة اللحظية:
    - `POST /api/v1/partner/inventory/sync`: مزامنة المخزون والأسعار بالجملة (Batch Sync).
    - `POST /api/v1/partner/inventory/update-item`: تحديث كمية صنف لحظياً عند البيع من الكاشير (Real-time POS Hook).
    - `GET /api/v1/partner/reservations`: سحب الحجوزات الجديدة الواردة من تطبيق المريض مباشرة إلى نظام الصيدلية.
    - `POST /api/v1/partner/reservations/{id}/fulfill`: تأكيد تسليم الحجز آلياً عند مسح QR العميل.
  - كتابة اختبار ميزة شامل `AdminAndPartnerApiTest` واجتياز 11 اختباراً بنجاح 100%.

## [2.6.0] - 2026-09-18
### تم الإصلاح والتحسين (Fixed & Enhanced)
- **منطق وحالة شاشة البحث الأولية (Refined Search Lifecycle & Pre-Search State):**
  - منع جلب وعرض نتائج البحث المسبقة تلقائياً قبل أن يقوم المستخدم بكتابة أي استعلام أو اختيار دواء.
  - إيقاف الاستدعاء التلقائي لـ `_fetchMedicines()` عند بدء تشغيل `HomeScreen`.
  - تصميم واجهة ما قبل البحث التفاعلية (`_buildPreSearchState`):
    - بطاقة توجيهية طبية أنيقة تشرح للمريض كيفية البحث عن الأدوية.
    - إضافة رقائق البحث السريع التفاعلية (Quick Search Chips) للأدوية الأكثر طلباً (`Panadol Extra`, `Augmentin`, `Brufen`, `Norvasc`).
    - النقر على أي رقاقة يملأ مربع البحث فورياً ويسترجع النتائج المطابقة من الصيدليات.
  - حصر ظهور قائمة النتائج الصيدلانية بالاستعلامات المكتوبة فعلياً من قِبل المستخدم، مع دعم زر الإلغاء/المسح الفوري (`clear`).

## [2.5.0] - 2026-09-18
### تم الإصلاح والتحسين (Fixed & Enhanced)
- **التحقق الصارم والأمني من المصادقة (Strict Backend Authentication & Verification):**
  - إزالة المحاكاة التلقائية (Mock Fallback Bypass) التي كانت تقبل أي بريد إلكتروني وهمي دون تحقق.
  - إلزام التحقق الفعلي الصارم عبر واجهة RESTful API (`/api/v1/auth/login` و `/api/v1/auth/register`) ومطابقة كلمات المرور المشفرة بـ `Hash::check()` في قاعدة بيانات الخادم.
  - استحداث صنف `AuthResult` لنقل تفاصيل ورسائل استجابة الخادم بدقة إلى واجهات المستخدم (مثل: «البريد الإلكتروني أو كلمة المرور غير صحيحة»، «البريد مسجل مسبقاً»، إلخ).
  - إضافة رسائل تحقق عربية مخصصة في `AuthController` بالخادم المركزي لتعزيز دقة التغذية الراجعة.
  - إضافة زر تجربة سريعة في شاشة الدخول يملأ بيانات الحساب التجريبي المعتمد المزرع في قاعدة البيانات (`patient@pharmaconnect.ye` / `password123`).
  - كتابة اختبار وحدة في Flutter يتحقق من رفض تسجيل الدخول لأي بيانات غير صحيحة وعدم فتح الجلسة إطلاقاً.

## [2.4.0] - 2026-09-18
### أضيف (Added)
- **منطق تدفق الدخول وبدء التشغيل (Entry Flow & Seamless Auth Landing):**
  - جعل شاشة المصادقة `AuthScreen` هي الشاشة الافتراضية عند فتح التطبيق لأول مرة للمستخدمين غير المسجلين.
  - إتاحة ثلاثة خيارات منطقية ومتسلسلة للعميل عند فتح التطبيق:
    1. **تسجيل الدخول (Login):** للمرضى المسجلين مسبقاً عبر البريد وكلمة المرور.
    2. **إنشاء حساب جديد (Register):** للمرضى الجدد مع الاسم الكامل ورقم الهاتف للتواصل.
    3. **المتابعة كزائر (Continue as Guest):** للبحث المباشر عن الأدوية للحالات الطارئة دون إجبار على التسجيل الفوري.
  - توجيه المستخدم تلقائياً إلى شاشة تسجيل الدخول `AuthScreen` عند تسجيل الخروج من شاشة الملف الشخصي.
  - تحديث واجتياز اختبارات الودجات `widget_test.dart` لتغطية مسار تدفق المصادقة والدخول كزائر بنجاح.

## [2.3.0] - 2026-09-18
### أضيف (Added)
- **الملف الشخصي والمصادقة للعميل (Client Profile & Authentication):**
  - بناء نموذج المستخدم `UserModel` لتمثيل بيانات المريض المسجل (الاسم، البريد، الهاتف، الدور).
  - تطوير شاشة الملف الشخصي الكاملة `ProfileScreen` متضمنة:
    - بطاقة بيانات المريض (الاسم، البريد، الهاتف، شارة التوثيق).
    - واجهة تسجيل الدخول وإنشاء حساب مريض جديد مدمجة بالأخضر الطبي.
    - إعدادات النطاق الجغرافي والإشعارات وزر تسجيل الخروج.
  - إضافة تبويب **«حسابي»** في شريط التنقل السفلي (`BottomNavigationBar`) وزر الوصول السريع في الـ AppBar.
  - ربط وتعبئة بيانات المريض آلياً في شاشة حجز الدواء عند تسجيل الدخول.

## [2.2.0] - 2026-09-18
### أضيف (Added)
- **خريطة المسار المباشر والملاحة التفاعلية (Visual Route Map & Live Navigation):**
  - بناء مكون `PharmacyRouteMapWidget` لرسم خريطة مسار تفاعلية برسم الطرق والمتجهات المباشرة بين موقع المريض وموقع الصيدلية المحددة.
  - إظهار نقطة موقع المريض الحالي 📍، ونقطة الصيدلية 🏥، ومسار الحركة اللحظي المقدر.
  - حساب وعرض مؤشرات التنقل المباشرة:
    - مسافة المسار بالكيلومتر (مثلاً: 1.2 كم).
    - الوقت التقديري للوصول بالسيارة (مثلاً: ~ 4 دقائق).
    - الوقت التقديري للوصول مشياً (مثلاً: ~ 14 دقيقة).
  - إضافة زر التنقل والملاحة المباشرة: **«فتح المسار والملاحة الحية (Google Maps)»** لفتح خرائط جوجل وتوجيه المريض خطوة بخطوة بالصوت والصورة نحو الصيدلية.
  - تضمين الخريطة ومسار الملاحة في كل من شاشة تفاصيل الدواء `MedicineDetailsScreen` وشاشة تذكرة الحجز الرقمية `ReservationPassScreen`.

## [2.1.0] - 2026-09-18
### أضيف (Added)
- **واجهة طلبات وحجوزات المريض المخصصة (Patient Reservations Screen):**
  - بناء شاشة `MyReservationsScreen` لاستعراض ومتابعة حجوزات المريض السابقة والنشطة مع شارات الحالات وتفاصيل الصيدليات والمبالغ والتذاكر الرقمية.
  - إضافة شريط التنقل السفلي (`BottomNavigationBar`) وزر علوي بالـ AppBar في `HomeScreen` للتبديل السلس بين شاشة البحث وشاشة طلباتي.
  - دعم مزامنة الكاش المحلي مع الـ Backend عبر دالة `getMyReservations()`.
- **معالجة وتصحيح الاتصال اللحظي بين الخادم والعميل (Client-Server Integration Fixes):**
  - تعديل `AppConstants.baseUrl` لدعم التوجيه الديناميكي التلقائي بين منصات سطح المكتب (Windows / Web / Linux / macOS) على `127.0.0.1:8000` ومحاكي الأندرويد على `10.0.2.2:8000`.
  - تحديث `ReservationApiController` ومسارات `api.php` لدعم الحجز المباشر للمرضى وتسجيله في قاعدة بيانات الخادم المركزية فورياً.

## [2.0.0] - 2026-09-18
### أضيف (Added)
- **إنجاز المرحلة السابعة: النشر والتوثيق الأكاديمي النهائي ودليل المناقشة (Phase 7 Deployment & Defense):**
  - **دليل النشر والتشغيل السحابي (`docs/DEPLOYMENT.md`):**
    - توثيق المعمارية المادية للإنتاج (Ubuntu Linux, Nginx, PHP 8.2-FPM, MySQL 8.0, Redis, SSL Let's Encrypt).
    - خطوات إعداد الأمان، جدار الحماية، وأذونات الملفات وتخزين الـ Caching.
    - خطوات جدولة الـ Cron Job لمراقبة الحجوزات المنتهية (TTL Expiry Worker).
    - خطوات بناء حزم تثبيت أندرويد الإنتاجية (`flutter build apk --release --split-per-abi`).
    - خطة النسخ الاحتياطي التلقائي اليومي واستعادة الكوارث (Disaster Recovery).
  - **التقرير الأكاديمي النهائي الشامل وفق APA 7th Edition (`docs/FINAL_PROJECT_REPORT.md`):**
    - صياغة البحث الكامل: المستخلص، المقدمة، الإطار النظري لهندسة الحلقات المغلقة، التحليل المعماري وتصميم 3-Tier، صياغة معادلة هافرسين الرياضية وأقفال التزامن التشاؤمية، النتائج التجريبية واجتياز 47 توكيداً واختباراً بنسبة 100%، وتوزيع المهام التكاملي لثلاثة أعضاء، والمراجع الأكاديمية الموثقة بـ APA 7.
  - **دليل وسيناريو المناقشة الأكاديمية والعرض الحي (`docs/DEFENSE_PRESENTATION_GUIDE.md`):**
    - خطة الـ 15 دقيقة المجدولة بمخطط Gantt لتوزيع الأدوار على الباحثين الثلاثة.
    - سيناريو العرض العملي الحي خطوة بخطوة أمام لجنة المناقشة.
    - بنك الأسئلة المتوقعة من أعضاء هيئة التدريس مع إجاباتها العلمية الدقيقة.

## [1.10.0] - 2026-09-18
### أضيف (Added)
- **إنجاز المرحلة السادسة: التكامل الشامل والاختبارات التلقائية والـ CI/CD (Phase 6 Integration & CI/CD):**
  - **اختبار التكامل لدورة العمل الكاملة (End-to-End Integration Testing):**
    - كتابة اختبار `EndToEndIntegrationTest` الشامل للتحقق من دورة حياة العملية البرمجية من البداية حتى النهاية:
      1. تعديل المخزون والأسعار من بوابة الصيدلية عبر الويب (`/pharmacy/inventory/{id}/update`).
      2. انعكاس التعديلات فورياً في محرك البحث الجغرافي (`/api/v1/medicines/search`).
      3. طلب حجز دواء من تطبيق المريض عبر الـ API مع قفل التزامن الذري وخصم المخزون واحتساب مهلة الـ 30 دقيقة.
      4. ظهور الحجز فورياً في قائمة طلبات الصيدلية وتأكيد الاستلام والتسليم (`/pharmacy/reservations/{id}/confirm`).
      5. اختبار انتهاء المهلة الزمنية (TTL Auto-Expiry) واستعادة الكميات المحجوزة آلياً للمخزون المتاح.
  - **خطوط أنابيب التكامل المستمر (GitHub Actions CI/CD Pipelines):**
    - تحسين `backend-ci.yml` لدعم الفحص السريع بـ SQLite In-Memory واختبارات Pint و PHPUnit بدون اعتماديات ثقيلة.
    - التحقق من `frontend-ci.yml` واجتياز `flutter pub get`, `flutter analyze`, `flutter test`.
  - **مؤشرات الجودة والاعتماد:**
    - اجتياز اختبارات **PHPUnit** بنسبة 100% (6 test suites, 40 assertions passed).
    - اجتياز فحص **Laravel Pint** بنسبة 100% خالي من أي عيوب تنسيقية.
    - اجتياز فحص **Flutter Analyze** واختبارات **Flutter Test** بنسبة 100% بدون أي تحذيرات.

## [1.9.0] - 2026-09-18
### أضيف (Added)
- **إنجاز المرحلة الخامسة: تطوير عميل الهاتف المحمول (Phase 5 Mobile Client - Flutter):**
  - **نماذج البيانات وتحويل الـ JSON (Data Models):**
    - بناء `MedicineSearchItem`, `MedicineInfo`, `PharmacyInfo` لتمثيل وتفكيك نتائج البحث والوفرة الجغرافية.
    - بناء `ReservationModel`, `ReservationItemModel` لمعالجة بيانات تذكرة الحجز ومهلة الـ TTL.
  - **خدمة الشبكة والاتصال (Network & Resilience Layer):**
    - بناء `ApiService` الموحد للاتصال بـ RESTful API Gateway عبر حزمة `http: ^1.2.0`.
    - توفير آلية صمود وFallback محاكية متطابقة مع قاعدة بيانات صنعاء لضمان تشغيل وسلاسة التطبيق في أي بيئة اتصال.
  - **شاشات وتجربة المستخدم الطبية (Medical UI/UX Screens):**
    - تحديث `HomeScreen` بالبحث التفاعلي اللحظي، بطاقات الأدوية والصيدليات المتوفرة مع حساب المسافات وأسعار الدواء.
    - بناء شاشة `MedicineDetailsScreen` لعرض المواصفات الدوائية والجرعات، وتنبيهات الوصفة الطبية، ومحدد كمية الحجز الذكي.
    - بناء شاشة `ReservationPassScreen` لتذكرة الحجز اللحظية برمز الاستجابة السريع ومؤقت عد تنازلي حي للـ TTL (30 دقيقة) وتحديث فوري للحالة عند الانتهاء.
  - **الاختبارات التلقائية وفحص الجودة:**
    - إضافة اختبارات الوحدة `models_and_flow_test.dart` لاختبار الـ JSON Deserialization وتصفية البحث.
    - اجتياز فحص **Flutter Analyze** بنسبة 100% (No issues found).
    - اجتياز اختبارات **Flutter Test** بالكامل (4/4 tests passed).

## [1.8.0] - 2026-09-18
### أضيف (Added)
- **إنجاز المرحلة الرابعة: تطوير الخادم وبوابة المخزون والـ APIs (Phase 4 Backend & Web Portal):**
  - **طبقة المستودعات والخدمات (Repository & Service Pattern):**
    - بناء واجهة `MedicineRepositoryInterface` وتطبيقها `EloquentMedicineRepository` متضمناً حساب المسافات الجغرافية الدقيقة بصيغة **Haversine Formula**.
    - بناء واجهة `ReservationRepositoryInterface` وتطبيقها `EloquentReservationRepository` لمعالجة استعادة المخزون بعد انتهاء مهلة الـ TTL.
    - بناء خدمة البحث الجغرافي `GeoSearchService` لدعم الفلترة المكانية والترتيب حسب المسافة أو السعر.
    - بناء خدمة الحجوزات `ReservationService` لدعم المعاملات الذرية وحماية التزامن وأقفال الصفوف (Pessimistic Locking `lockForUpdate`) وتوليد أكواد الحجز `RES-XXXXXX` واحتساب مهلة الـ 30 دقيقة.
    - ربط المستودعات بالحاوية في `AppServiceProvider`.
  - **واجهات برمجة التطبيقات (Centralized RESTful APIs - v1):**
    - بناء `MedicineSearchResource` و `ReservationResource` لتوحيد بنية الاستجابات JSON.
    - تطوير متحكمات الـ API: `AuthController`, `MedicineSearchController`, `ReservationApiController`.
    - تفعيل 9 مسارات RESTful للبحث والحجز وإدارة التوكنات بـ Laravel Sanctum.
  - **بوابة الويب لإدارة مخزون الصيدليات (Multi-Tenant Web Portal):**
    - تطوير متحكم إدارة المخزون `PharmacyInventoryController` ومتحكم الجلسات `WebAuthController`.
    - بناء الواجهات الطبية بـ Blade بالأخضر الزمردي والأبيض الصافي:
      - `layouts/pharmacy.blade.php`: القالب الرئيسي المتجاوب.
      - `pharmacy/inventory.blade.php`: جدول المخزون وإحصائيات الأصناف والتعديل الفوري للكميات والأسعار.
      - `pharmacy/reservations.blade.php`: شاشة طلبات الحجز الواردة ومؤقتات العد التنازلي وتأكيد الاستلام.
      - `auth/login.blade.php`: شاشة تسجيل دخول الصيدلية.
  - **الاختبارات التلقائية وضمان الجودة:**
    - كتابة اختبارات الميزات: `GeoSearchApiTest` و `ReservationConcurrencyTest` لاختبار أقفال التزامن ومنع الحجز المزدوج.
    - اجتياز اختبارات **PHPUnit** كاملة بنسبة 100% (5 passed, 17 assertions).
    - اجتياز فحص **Laravel Pint** بنسبة 100% بدون أي أخطاء تنسيقية.

## [1.7.0] - 2026-09-18
### أضيف (Added)
- إنجاز المرحلة الثالثة: إعداد وتهيئة بيئات العمل البرمجية (Laravel 11, Flutter 3.41, 7 Migrations, 7 Eloquent Models, Database Seeder).

## [1.6.0] - 2026-09-18
### أضيف (Added)
- إنجاز المرحلة الثانية: وثيقة التصميم المعماري والنمذجة وقواعد البيانات `docs/ARCHITECTURE_AND_DESIGN.md`.

## [1.5.0] - 2026-09-18
### أضيف (Added)
- إنجاز المرحلة الأولى: وثيقة مواصفات متطلبات النظام الرسمية `docs/SRS.md`.

## [1.4.0] - 2026-09-18
### أضيف (Added)
- خريطة طريق وخطوات العمل التنفيذية التفصيلية `docs/ROADMAP.md`.

## [1.3.0] - 2026-09-18
### أضيف (Added)
- منظومة CI/CD واختبارات الجودة ومراجعة الكود وهيكلة المجلدات.

## [1.2.0] - 2026-09-18
### أضيف (Added)
- دستور المشروع الأصلي لمحرك Gemini: `GEMINI.md`.

## [1.1.0] - 2026-09-18
### معدل (Changed)
- اعتماد معمارية تعدد الصيدليات (Multi-Pharmacy / Multi-Tenant System).

## [1.0.0] - 2026-09-18
### أضيف (Added)
- صياغة وثيقة مقترح المشروع المعتمد `مقترح_المشروع_المعتمد.md`.
